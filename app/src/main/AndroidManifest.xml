<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mworkstation.com.mworkstation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".jason2view.JasonViewActivity" />
        <activity android:name=".app2install.AppInstallActivity" />
        <activity android:name=".encrypt2spref.DataEncyptActivity" />
        <activity android:name=".dateutil.DateActivity" />
        <activity android:name=".MatchGame.MatchActivity" />
        <activity android:name=".text2speech.Text2speechActivity"
            android:theme="@style/AppTheme2"/>
        <activity android:name=".actionbar2trans.TransparentActionbarActivity"
            android:theme="@style/AppTheme2"
            ></activity>
    </application>

</manifest>